//Write a program that takes a string of comma-separated words (e.g., "apple,banana,cherry") and converts it into a list of words using split. Then join the list back into a single string, separated by hyphens ("-").
const a ="apple,banana,cherry"
console.log(a.split(',').join('-'))