//The map and filter methods are also array methods, but they are non-mutating. This means they don’t change the original array; instead, they create a new array based on the transformation or filtering criteria.

